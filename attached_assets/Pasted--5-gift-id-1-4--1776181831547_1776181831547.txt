Двойная тема сайта. Этап 5 — страница подарка /gift/:id и кнопка «Согласовать подарок».
Этапы 1-4 сделаны: бэкенд роут /gift/:id работает, giftSuggestion есть, контент разделён по полу. Теперь нужно сделать механику подарков на фронтенде.
1. СТРАНИЦА ТОВАРА /product/:id — кнопка «Согласовать подарок»:
На обычной странице товара, если у товара giftSuggestion === true, добавить под основной кнопкой покупки блок:
Кнопка + подсказка в одну строку:

Кнопка «Согласовать подарок 🎁»:

Стиль: outlined — прозрачный фон, бордер 1px solid var(--color-gift-accent), цвет текста var(--color-gift-accent)
Border-radius: 8px
Padding: 10px 24px
Font-size: 13px, font-weight: 500
Hover: фон var(--color-gift-bg), лёгкий подъём (translateY(-1px))
При клике — вызвать нативный диалог «Поделиться» браузера (Web Share API):



javascript    navigator.share({
      title: `${brand} ${name} — PickMe Store`,
      text: 'Посмотри, хочу такой подарок!',
      url: `https://pickmestore.ru/gift/${id}`
    })

Если браузер не поддерживает Web Share API (десктоп) — скопировать ссылку https://pickmestore.ru/gift/${id} в буфер обмена и показать уведомление «Ссылка скопирована ✓» на 2 секунды (текст кнопки временно меняется)
Иконка подсказки — справа от кнопки, значок ❓ в кружке:

Кружок: width: 22px, height: 22px, border-radius: 50%, бордер 1px solid var(--color-border), цвет var(--color-text-muted)
Font-size: 12px
Cursor: pointer
При наведении (hover) или при тапе на мобильном — показать всплывающую подсказку (tooltip):

Текст: «Нажми, чтобы поделиться ссылкой на этот товар. Получатель увидит фото и описание, но цена будет скрыта — удобно, чтобы согласовать подарок.»
Стиль: фон var(--color-card-bg), бордер 1px solid var(--color-border), border-radius: 8px, padding: 12px 16px, font-size: 12px, цвет var(--color-text-body), max-width: 280px
Позиция: над или сбоку от иконки, с маленьким треугольником-стрелкой
На мобильном: тултип появляется по тапу и скрывается по тапу в любое другое место
box-shadow: 0 4px 16px rgba(0,0,0,0.08)





Если у товара giftSuggestion === false — кнопку и подсказку НЕ показывать.
2. РОУТ НА ФРОНТЕНДЕ — /gift/:id:
Добавь React-роут /gift/:id — отдельная страница, аналогичная /product/:id по структуре, но с изменённым содержимым.
3. СТРАНИЦА /gift/:id — что показывать:
Страница загружает товар через GET /api/products/:id (тот же эндпоинт).
Отображать:

Фото товара (карусель если несколько фото — как на обычной странице товара)
Бренд — цвет var(--color-gift-accent), uppercase, letter-spacing
Название товара — шрифт var(--font-heading), крупно
Размер (если есть)
Описание товара (если есть)
Замеры (если есть — outerSeam, innerSeam и т.д.)

НЕ отображать:

Цену — нигде на странице, ни в каком виде
Цену закупки (purchasePrice)
Артикул (sku)
Ссылку на Авито (avitoLink)
Статус (available/sold)
Кнопку «Купить»
Кнопку «Согласовать подарок»

4. ПЛАШКА «ПОДАРОЧНАЯ ССЫЛКА»:
Над фото товара — небольшая плашка:

Текст: «🎁 Подарочная ссылка — цена скрыта»
Фон: var(--color-gift-bg)
Бордер: 1px solid var(--color-gift-border)
Border-radius: 8px
Padding: 8px 16px
Font-size: 13px
Цвет текста: var(--color-gift-accent)